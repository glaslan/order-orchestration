spring:
  application:
    name: order
  datasource:
    url: jdbc:postgresql://localhost:5432/orderdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

jwt:
  secret: ${JWT_SECRET:farm2fork-orders-dev-secret-key!}

server:
  servlet:
    session:
      cookie:
        http-only: true
        same-site: lax

teams:
  customer:
    url: ${CUSTOMER_URL:http://147.182.145.77:3000}
  delivery:
    url: ${DELIVERY_URL:http://147.182.145.77:3000}
